package studentdirectory.controller;

public class FileController {
  //File read and write
  public static void writeUserDetailsToFile(){
    //Fetch list from user collection
    //Open the file and start writing and if file not present then create it
    //If directory not found then error
  }

  public static void readUserDetailsFromFile(){
    //Open the file and start reading and if file not present then skip
    //append the objects in user collection using user controller
    //If directory not found then error
  }
}
